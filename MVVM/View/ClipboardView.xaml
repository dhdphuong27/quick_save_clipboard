<UserControl x:Class="Quicksave_Clipboard.MVVM.View.ClipboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModel="clr-namespace:Quicksave_Clipboard.MVVM.ViewModel"
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:core="clr-namespace:Quicksave_Clipboard.Core"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             d:DesignHeight="640" d:DesignWidth="810"
             d:DataContext="{d:DesignInstance Type=viewModel:ClipboardViewModel}">
    <UserControl.Resources>
        <core:PageComparisonConverter x:Key="PageComparisonConverter"/>
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search Box -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,15">
            <TextBox Width="400" Style="{DynamicResource ModernTextbox}"/>
            <Button Style="{DynamicResource SearchButton}">
                <Icon:PackIconMaterial Kind="Magnify" Style="{DynamicResource searchIcon}"/>
            </Button>
        </StackPanel>

        <!-- Quick Paste Button -->
        <Button Grid.Row="1" Command="{Binding SaveContentCommand}" 
                Style="{StaticResource quickSaveButton}" Content="Quick Paste"
                HorizontalAlignment="Right" Margin="0,0,30,15"/>

        <!-- Notification -->
        <!--<TextBlock Grid.Row="1" Text="{Binding TestMsg}" 
                   Style="{DynamicResource NotificationTextblock}" Margin="0,0,0,15"/>-->
        <DataGrid Grid.Row="2" Style="{DynamicResource ModernClipboardDataGridStyle}"
                      ItemsSource="{Binding PagedData}" 
                      CellStyle="{DynamicResource ModernDataGridCellStyle}"
                      ColumnHeaderStyle="{DynamicResource ModernDataGridColumnHeaderStyle}"
                      SelectedItem="{Binding SelectedDataItem}"
                      AutoGenerateColumns="False"
                      CanUserResizeColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      >
            
            <DataGrid.Columns>
                <!-- Content Column -->
                <DataGridTextColumn Header="Content" Width="376">
                    <DataGridTextColumn.Binding>
                        <Binding Path="PreviewText" Mode="OneWay"/>
                    </DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource CellContent}">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="Padding" Value="12,6"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Saved_date Column -->
                <DataGridTextColumn Header="Date created" Binding="{Binding DateCreated, StringFormat={}{0:dd/MM/yyyy}}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource CellContent}">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="Padding" Value="12,6"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Type Column -->
                <DataGridTextColumn Header="Type" Binding="{Binding ContentType}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource CellContent}">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="Padding" Value="12,6"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Status Column -->
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource CellContent}">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="Padding" Value="12,6"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Actions Column -->
                <DataGridTemplateColumn Header="Actions" Width="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Command="{Binding DataContext.ViewCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{DynamicResource gridButton}" 
                                        ToolTip="View"
                                        Tag="ViewContent">
                                    <Icon:PackIconMaterial Kind="EyeOutline" Style="{DynamicResource gridButtonIcon}"/>
                                </Button>
                                <Button Command="{Binding DataContext.CopyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{DynamicResource gridButton}" 
                                        ToolTip="Copy"
                                        Tag="ContentCopy">        
                                    <Icon:PackIconMaterial Kind="ContentCopy" Style="{DynamicResource gridButtonIcon}"/>
                                </Button>

                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Pagination -->
        <Border Grid.Row="4" Style="{DynamicResource PaginationBorder}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource PaginationLeftButton}" Command="{Binding LoadPreviousPageCommand}"/>
                <ItemsControl ItemsSource="{Binding Buttons}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding page}"
                                    Command="{Binding DataContext.SelectPageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource PaginationNumberButton}">            
                                <Button.Tag>
                                    <MultiBinding Converter="{StaticResource PageComparisonConverter}"
                                                  Mode="OneWay">
                                        <Binding Path="page" />
                                        <Binding Path="DataContext.CurrentPageNumber" 
                                                 RelativeSource="{RelativeSource AncestorType=UserControl}" 
                                                 UpdateSourceTrigger="PropertyChanged"/>
                                    </MultiBinding>
                                </Button.Tag>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button Style="{StaticResource PaginationRightButton}" Command="{Binding LoadNextPageCommand}"/>
            </StackPanel>
        </Border>
        
    </Grid>
</UserControl>